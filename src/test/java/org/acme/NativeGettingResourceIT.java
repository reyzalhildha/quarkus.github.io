package org.acme;

import io.quarkus.test.junit.NativeImageTest;

@NativeImageTest
public class NativeGettingResourceIT extends GettingResourceTest {

    // Execute the same tests but in native mode.
}